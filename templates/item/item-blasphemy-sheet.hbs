<form class="{{cssClass}} main-blasphemy" style="--primary-color: {{system.primaryColor}}; --accent-color: {{system.accentColor}}; --secondary-color: {{system.secondaryColor}}; --text-color: {{system.textColor}};">
  <header class="sheet-header">
    <img class="profile-img" src="{{item.img}}" data-edit="img" title="{{system.blasphemyName}}"/>
    <div class="header-fields">
      <label for="name">Blasphemy Name</label>
      <input type="text" name="system.blasphemyName" class="charname blasphemy-text" value="{{system.blasphemyName}}" data-dtype="String"/>
    </div>
  </header>

  {{!-- Sheet Tab Navigation --}}
  <nav class="sheet-tabs tabs" data-group="primary">
    <a class="item" data-tab="passive">Passive</a>
    <a class="item" data-tab="powers">Powers</a>
  </nav>

  {{!-- Sheet Body --}}
  <section class='sheet-body'>
    <div class='tab' data-group='primary' data-tab='passive'>
      {{#each blasphemyPassives}}
        {{> blasphemyPowerPartial this powerItem=this developerMode=../developerMode}}
      {{/each}}
      <div class="color-picker-container">
        <button class="color-picker-toggle" type="button">
          <i class="fas fa-palette"></i>
        </button>
        <div class="color-pickers">
          <input type="color" id="primaryColor" data-id="primary-color" value="{{system.primaryColor}}" title="Primary Color">
          <input type="color" id="accentColor" data-id="accent-color" value="{{system.accentColor}}" title="Accent Color">
          <input type="color" id="secondaryColor" data-id="secondary-color" value="{{system.secondaryColor}}" title="Secondary Color">
          <input type="color" id="textColor" data-id="text-color" value="{{system.textColor}}" title="Text Color">
        </div>
      </div>
    </div>
    <div class='tab' data-group='primary' data-tab='powers'>
      {{#each blasphemyPowers}}
        {{> blasphemyPowerPartial this powerItem=this developerMode=../developerMode}}
      {{/each}}
    </div>
  </section>
</form>

<style>
@import url('https://fonts.googleapis.com/css2?family=Lacquer&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Courier+New&display=swap');

/* Dark Theme */
.main-blasphemy {
  background-color: var(--primary-color);
  color: var(--text-color);
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.5);
  font-family: 'Courier New', Courier, monospace;
}

.main-blasphemy .sheet-header {
  display: flex;
  align-items: center;
  background-color: var(--secondary-color);
  border-radius: 5px;
  padding: 10px;
}

.main-blasphemy .profile-img {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  margin-right: 20px;
  object-fit: cover;
  border: 2px solid #555;
}

.main-blasphemy .header-fields {
  flex-grow: 1;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.main-blasphemy .charname {
  font-size: 1.5em;
  margin: 0;
  margin-bottom: 10px;
  color: var(--text-color);
}

.main-blasphemy .form-group {
  margin-bottom: 10px;
}

.main-blasphemy .form-group label {
  display: block;
  font-weight: bold;
  margin-bottom: 5px;
  font-family: 'Lacquer', sans-serif;
}

.main-blasphemy .form-group input[type="text"],
.main-blasphemy .form-group input[type="checkbox"],
.main-blasphemy .form-group select {
  width: 100%;
  padding: 8px;
  border: 1px solid var(--secondary-color);
  border-radius: 4px;
  box-sizing: border-box;
  background-color: var(--primary-color);
  color: var(--text-color);
  font-family: 'Courier New', Courier, monospace;
}

.main-blasphemy .form-group input[type="checkbox"] {
  width: auto;
  margin-right: 10px;
}

.main-blasphemy .sheet-body {
  overflow-y: auto;
  background-color: #333;
  padding: 15px;
  border-radius: 8px;
  border: 1px solid #555;
  position: relative;
}

.main-blasphemy .sheet-body p {
  display: block;
  font-size: 1em;
  line-height: 1.5;
  color: var(--text-color);
}

.main-blasphemy .sheet-body h2 {
  color: var(--accent-color);
  text-shadow: 0 0 10px var(--accent-color);
  margin-bottom: 10px;
  font-family: 'Lacquer', sans-serif;
}

.main-blasphemy .passive-section,
.main-blasphemy .powers-section {
  margin-bottom: 20px;
  background-color: var(--primary-color);
  color: var(--text-color);
  padding: 10px;
  border-radius: 5px;
  border: 1px solid var(--secondary-color);
}

.main-blasphemy .profile-img {
  border: none;
}

.main-blasphemy .charname {
  font-size: 24px;
  font-weight: bold;
}

.main-blasphemy .color-picker-container {
  display: flex;
  align-items: center;
  gap: 10px;
  position: relative;
}

.main-blasphemy .color-picker-toggle {
  background-color: var(--primary-color, #2a2a2a);
  color: var(--text-color, #ffffff);
  border: 2px solid var(--secondary-color, #333);
  padding: 5px;
  cursor: pointer;
  transition: transform 0.3s ease, border-radius 0.3s ease, box-shadow 0.3s ease;
  box-shadow: 0 0 10px var(--accent-color, #ff00ff), 0 0 20px var(--accent-color, #ff00ff);
}

.main-blasphemy .color-picker-toggle:hover {
  transform: scale(1.05);
  box-shadow: 0 0 20px var(--accent-color, #ff00ff), 0 0 40px var(--accent-color, #ff00ff);
}

.main-blasphemy .color-pickers {
  display: flex;
  flex-direction: row;
  gap: 10px;
  transition: transform 0.5s ease, opacity 0.5s ease;
  transform: translateY(-20px);
  opacity: 0;
  pointer-events: none;
  position: absolute;
  left: 0;
  top: 100%;
}

.main-blasphemy .color-picker-container.active .color-pickers {
  margin-top: 20px;
  transform: translateY(0);
  opacity: 1;
}

.main-blasphemy .color-pickers input[type="color"] {
  width: 40px;
  height: 40px;
  border: none;
  cursor: pointer;
  border-radius: 50%;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.5), 0 0 10px var(--accent-color, #ff00ff);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.main-blasphemy .color-pickers input[type="color"]:hover {
  transform: scale(1.2);
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.5), 0 0 20px var(--accent-color, #ff00ff);
}

.main-blasphemy .sheet-tabs {
  display: flex;
  justify-content: space-around;
  border-bottom: 1px solid var(--secondary-color);
  padding-bottom: 10px;
  padding-top: 10px;
  font-family: 'Courier New', Courier, monospace;
}

.main-blasphemy .sheet-tabs .item {
  color: var(--text-color);
  background-color: var(--primary-color);
  border: 1px solid var(--secondary-color);
  padding: 10px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
  font-family: 'Courier New', Courier, monospace;
}

.main-blasphemy .sheet-tabs .item.active {
  background-color: var(--secondary-color);
}

.main-blasphemy .tab {
  display: none;
}

.main-blasphemy .tab.active {
  display: block;
}
</style>