<div class="tab palace-pressure" data-group="primary" data-tab="palace-pressure">
  <div class="form-grid">
    <div class="form-group dark-form-group full-width">
      <label for="system.palace" class="centered-label">Palace</label>
      {{ editor enrichedPalace target='system.palace' engine='prosemirror' button=true editable=editable class="mob-psycho-editor" }}
    </div>
    <hr class="divider">
    <div class="form-group dark-form-group full-width">
      <label for="system.pressure" class="centered-label">Pressure</label>
      {{ editor enrichedPressure target='system.pressure' engine='prosemirror' button=true editable=editable class="mob-psycho-editor" }}
    </div>
  </div>
</div>

<style>
@import url('https://fonts.googleapis.com/css2?family=Courier+New:wght@400;700&display=swap');

.tab.palace-pressure {
  background-color: #1a1a1a;
  color: #f5f5f5;
  padding: 20px;
  font-family: 'Courier New', monospace;
}

.tab.palace-pressure .form-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 20px;
}

.tab.palace-pressure .form-group {
  text-align: center;
}

.tab.palace-pressure .dark-form-group label {
  color: #f5f5f5;
  font-weight: bold;
  display: block;
  margin-bottom: 10px;
}

.tab.palace-pressure .centered-label {
  text-align: center;
  font-family: 'Courier New', monospace;
  font-weight: 700;
  font-size: 1.4em; /* Larger font size */
  letter-spacing: 0.1em; /* Increased spacing between letters */
}

.tab.palace-pressure .large-textarea {
  width: 100%;
  height: 150px;
  padding: 10px;
  background-color: #333;
  color: #f5f5f5;
  border: 1px solid #444;
  border-radius: 4px;
}

.tab.palace-pressure .mob-psycho-editor {
  width: 100%;
  height: 200px; /* Increased height for better editing experience */
  padding: 10px;
  background-color: #333;
  color: #f5f5f5;
  border: 1px solid #444;
  border-radius: 4px;
  overflow-y: auto;
}

.tab.palace-pressure .full-width {
  grid-column: span 1;
  height: 200px; /* Increased height for better editing experience */
}

form .tab.palace-pressure .form-group {
  display: block;
  margin-bottom: 20px;
}

.tab.palace-pressure .divider {
  border: 0;
  height: 1px;
  background: #444;
  margin: 20px 0;
}

.tab.palace-pressure input[type="checkbox"] {
  width: 20px;
  height: 20px;
  margin: 0;
  padding: 0;
  border: 1px solid #444;
  border-radius: 4px;
  background-color: #333;
  color: #f5f5f5;
  font-family: 'Courier New', monospace;
  position: absolute;
  left: -30px;
  cursor: pointer;
  transition: transform 0.3s ease, background-color 0.3s ease;
}

.tab.palace-pressure input[type="checkbox"]:checked {
  background-color: #00ff00;
  transform: rotate(20deg);
}

.tab.palace-pressure input[type="checkbox"]:checked + .trauma-question-input {
  text-decoration: line-through;
}

.tab.palace-pressure input[type="text"] {
  width: 100%;
  padding: 10px;
  background-color: #333;
  color: #f5f5f5;
  border: 1px solid #444;
  border-radius: 4px;
  font-family: 'Courier New', monospace;
  transition: background-color 0.3s ease, color 0.3s ease;
}
</style>